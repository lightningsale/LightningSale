{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="card">
            <div class="card-header">Config</div>
            <div class="card-body">
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}